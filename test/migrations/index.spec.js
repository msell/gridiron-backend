var migrate = require('../../migrations'),
    request = require('supertest'),
    expect = require('expect'),
    chai = require('chai');


describe('DB Migrations', function () {
    describe('when database already exists', function () {
        it('should not import teams')
    });
});